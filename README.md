# Mantos

This repo is about a project that calls "Mantos". Mantos is an API to provide data about soccer jerseys making a request to a given endpoint, like:

```https://www.mantos-api.com/search?club=vasco```

and return a response in JSON containing all Vasco jerseys existing in database.